dependencies:
    pre:
        - sudo apt-get update
        - sudo apt-get install cmake
        
compile:
    override:
        - cmake ./module1
        
test:
    override:
        - echo "hello world"
