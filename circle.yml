dependencies:
    pre:
        - sudo apt-get update
        - sudo apt-get install cmake
        
compile:
    override:
        - cd module1
        - mkdir out
        - cd out
        - cmake ..
        
test:
    override:
        - echo "hello world"
