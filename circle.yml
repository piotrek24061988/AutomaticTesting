dependencies:
    pre:
        - wget https://cmake.org/files/v3.9/cmake-3.9.0-rc5.tar.gz
        - tar xzvf cmake-3.9.0-rc5.tar.gz
        - cd cmake-3.9.0-rc5/
        - echo ${PWD}
        - cmake .
        - make      
        - make install
compile:
    override:
        - cmake ./module1
        
test:
    override:
        - echo "hello world"
